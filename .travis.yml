language: crystal
crystal:
  - latest

env:
  - DATABASE_URL="mysql://root@localhost/test"

services:
  - mysql

before_install:
  - DATABASE_URL=$DATABASE_URL mysql -u root --password="" < test-data/setup.sql

script:
  - crystal spec
